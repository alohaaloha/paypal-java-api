package travelsafe.business.rules;

import travelsafe.model.TravelInsurance

rule "Initial price"
dialect "mvel"
    when
        $travelInsurance : TravelInsurance()
    then
        $travelInsurance.setTotalPrice(new Double(10.0));
    end